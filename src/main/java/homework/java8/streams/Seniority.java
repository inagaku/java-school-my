package homework.java8.streams;

import lombok.AllArgsConstructor;

/**
 * @author Evgeny Borisov
 */

public enum Seniority {
    JUN(0, 10),MIDDLE(11, 20),SENIOR(31, Integer.MAX_VALUE);

    Seniority(int i, int i1) {
    }
}
